class Fajax{
    constructor(){
        this.openFajax(typeOfRequest,url,content)
        {
            //returns ["GET",url]/["POST",url,content] based on what the client does
        },
        this.sendFajax()
        {},
        this.onloadFajax()
        {} 
        // when it gets the action from the client it starts the connection


        
    }
}